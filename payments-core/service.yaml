apiVersion: v1
kind: Service
metadata:
  name: payments-core-service
  namespace: novapay
spec:
  selector:
    app: payments-core
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP